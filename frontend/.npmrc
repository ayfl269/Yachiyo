# frontend 是独立于根 workspace 的包，安装时不应继承根 pnpm-workspace.yaml 的配置（尤其 overrides）
# 否则 pnpm 11.5.3 的 install/runDepsStatusCheck 会读到根 workspace 的 overrides
# 而 frontend/pnpm-lock.yaml 没有 overrides 字段 → ERR_PNPM_LOCKFILE_CONFIG_MISMATCH
# CI 中 pnpm install --frozen-lockfile --ignore-workspace 命令行参数确保 install 阶段忽略 workspace overrides
# CI 中 npx tsc -b && npx vite build 绕过 pnpm run 的 runDepsStatusCheck（见 ci.yml frontend-checks 注释）
ignore-workspace=true
